# Railway: Root Directory 를 cms 로 둔 서비스 전용 (API 서비스와 분리)

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
dependsOn = ["setup"]
cmds = ["npm ci --ignore-engines"]

[phases.build]
dependsOn = ["install"]
cmds = ["npm run build"]
