# (선택) CMS만 별도 서비스로 올릴 때 사용. 기본은 API 서비스가 /admin 에 CMS를 같이 서빙.
# 빌드 시 VITE_API_URL=https://<API>/api 필요.

[build]
builder = "nixpacks"

[deploy]
startCommand = "./node_modules/.bin/serve dist -s -l tcp://0.0.0.0:$PORT"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
